---
// src/components/LoopComponents/Video/Video.astro
import VideoClient from './VideoClient.jsx';

const { video, title = "Video", itemClass = "", featuredImage } = Astro.props;
---
<VideoClient 
  video={video} 
  title={title} 
  itemClass={itemClass} 
  featuredImage={featuredImage} 
  client:idle 
/>
