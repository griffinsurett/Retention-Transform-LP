---
// src/components/LoopComponents/Video/Video.astro
import VideoClient from './VideoClient.jsx';

const { video, title = "Video", itemClass = "", thumbnail } = Astro.props;
---
<VideoClient 
  video={video} 
  title={title} 
  itemClass={itemClass} 
  thumbnail={thumbnail} 
  client:idle 
/>
