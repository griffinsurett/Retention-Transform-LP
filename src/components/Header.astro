---
import { SiteData } from '@/content/SiteData';
import { Picture } from 'astro:assets';
---
<header class="header w-full min-h-1/3 flex justify-center items-center" role="banner">
  <a href="/" aria-label="Home">
    <Picture 
      src={SiteData.logo.src} 
      alt={SiteData.title} 
      formats={['avif', 'webp']} 
      fallbackFormat="png" 
      loading="eager" 
      fetchpriority="high" 
      width={380} 
      height={209} 
    />
  </a>
</header>
